# Your timezone, https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=#Europe/London

# UNIX PUID and PGID, find with: id $USER
PUID=#1000
PGID=#1000

# The directory where data and configuration will be stored.
CONFIG=#./config
DOWNLOADS=#./downloads
MEDIA=#./media

# --- Tailscale (Options B & C) ---
# Generate a reusable auth key at https://login.tailscale.com/admin/settings/keys
#TS_AUTHKEY=#tskey-auth-xxxx
# Your tailnet domain (e.g. tail12345.ts.net), found at https://login.tailscale.com/admin/dns
# Also used as a fallback in Dashy when using Cloudflare (Option C)
#TS_DOMAIN=#tail12345.ts.net

# --- Cloudflare (Option C) ---
# Your domain - services accessible at jellyfin.example.com, sonarr.example.com, etc.
#DOMAIN=#example.com
# Cloudflare API token: https://dash.cloudflare.com/profile/api-tokens (Zone > DNS > Edit)
#CF_API_TOKEN=#your-cloudflare-api-token
# Cloudflare Zone ID: found on your domain's overview page in the Cloudflare dashboard (right sidebar)
#CF_ZONE_ID=#your-cloudflare-zone-id

# --- Mullvad WireGuard VPN (optional) ---
# Routes download traffic (Deluge, Prowlarr, Sonarr, Radarr) through Mullvad.
# Enable by adding: COMPOSE_FILE=docker-compose.yml:docker-compose.vpn.yml
# Generate a config at https://mullvad.net/en/account/wireguard-config
#WIREGUARD_PRIVATE_KEY=#your-base64-private-key
#WIREGUARD_ADDRESSES=#10.x.x.x/32
#VPN_CITY=#Zurich
#COMPOSE_FILE=#docker-compose.yml:docker-compose.vpn.yml

# --- Default credentials ---
# Update these after changing passwords in each app's UI.
# Shown as tooltips on the Dashy dashboard.
DELUGE_USER=admin
DELUGE_PASS=deluge
PROWLARR_USER=admin
PROWLARR_PASS=change-me
SONARR_USER=admin
SONARR_PASS=change-me
RADARR_USER=admin
RADARR_PASS=change-me
JELLYFIN_USER=admin
JELLYFIN_PASS=change-me
FILEBROWSER_USER=admin
FILEBROWSER_PASS=admin
PORTAINER_USER=admin
PORTAINER_PASS=change-me

# --- Enable remote access via Caddy (Options B & C) ---
#COMPOSE_PROFILES=#remote